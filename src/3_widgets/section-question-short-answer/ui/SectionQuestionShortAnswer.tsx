import { Section } from '@/6_shared/ui/section'
import { SectionTitle } from '@/6_shared/ui/section-title'

export const SectionQuestionShortAnswer = () => {
    return (
        <Section>
            <SectionTitle>
                Краткий ответ
            </SectionTitle>
            
            Секция с коротким ответом на вопрос.
        </Section>
    )
}